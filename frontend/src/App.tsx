function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Finance Dashboard</p>
        <h1>React + TypeScript is ready.</h1>
        <p className="description">
          The frontend has been initialized with Vite so we can start building the
          dashboard UI from here.
        </p>
      </section>
    </main>
  );
}

export default App;
